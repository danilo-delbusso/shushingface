linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused
    - misspell
    - gosimple

linters-settings:
  errcheck:
    check-blank: false

issues:
  exclude-dirs:
    - frontend
